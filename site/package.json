{
  "name": "site",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "sync-rfcs": "node scripts/sync-rfcs.mjs",
    "test": "node --test scripts/*.test.mjs",
    "dev": "npm run sync-rfcs && astro dev",
    "start": "npm run dev",
    "build": "npm run sync-rfcs && astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.41.4",
    "astro": "^7.0.2",
    "sharp": "^0.35.3"
  }
}
